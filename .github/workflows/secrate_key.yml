name: Testing secrate variables

on:
  workflow_dispatch:

env:
  ACTIONS_STEP_DEBUG: true 

jobs:
  test_context_variables:
    runs-on: ubuntu-latest

    steps:
      - name: "checking secrate variables"
      
        run:
        
        echo "secrate value: ${{secrate.MY_KEY}} "
